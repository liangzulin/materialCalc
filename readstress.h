#ifndef READSTRESS_H
#define READSTRESS_H
#include <string>    // For string class
#include <vector>
using namespace std;
bool isHead(const char*, string* const);
vector<float>* readfile(const char* );

#endif
